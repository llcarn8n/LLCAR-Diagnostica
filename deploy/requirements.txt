# LLCAR Diagnostica KB — Python Dependencies
# Install order:
#   1. torch (see install-torch.bat or step 2 in deploy.bat)
#   2. pip install -r requirements.txt

# ─── API server ───────────────────────────────────────────────
fastapi==0.134.0
uvicorn[standard]==0.41.0
pydantic==2.12.5
aiofiles==25.1.0

# ─── Vector DB ────────────────────────────────────────────────
lancedb==0.29.2

# ─── Embeddings & ML ─────────────────────────────────────────
transformers==5.2.0
sentence-transformers==5.2.3
FlagEmbedding==1.3.5
accelerate==1.12.0
safetensors==0.7.0
tokenizers==0.22.2
sentencepiece==0.2.1
huggingface-hub==1.3.5
numpy==2.3.5
scipy==1.17.0
scikit-learn==1.8.0
pillow==12.0.0

# ─── HTTP / async ─────────────────────────────────────────────
httpx==0.28.1
requests

# ─── Scrapers (optional — only needed to re-scrape) ──────────
# scrapling==0.4.1
# patchright==1.56.0

# ─── Fine-tuning evaluation (optional) ───────────────────────
# datasets==4.6.1
# evaluate==0.4.6
# sacrebleu==2.6.0

# ─── Utilities ────────────────────────────────────────────────
tqdm
